type DenoBlob = Blob
type DenoURL = URL
type DenoURLSearchParams = URLSearchParams
type DenoReadableStream = ReadableStream

export type {
  DenoBlob as Blob,
  DenoURL as URL,
  DenoURLSearchParams as URLSearchParams,
  DenoReadableStream as ReadableStream,
}
